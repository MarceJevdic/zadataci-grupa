#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cctype>
using namespace std;

void funkcija(vector<int> v,int k) {
	
	for (int i = 0; i < k; i++) {
		v.insert(v.end(), v[0]);
		v.erase(v.begin());
		
	}
	for (int x : v) {
		cout << x << " ";
	}
}
int main(){
	vector<int> v = { 1,2,3,4,5 };
	int k = 2;
	funkcija(v,k);
	return 0;
}
