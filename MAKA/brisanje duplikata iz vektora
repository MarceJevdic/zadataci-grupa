#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;
void funkcija(vector<int>& v) {
	sort(v.begin(), v.end());
	int i = 0;
	while (i < v.size()) {
		int vrijednost = v.at(i);
		auto it=remove(v.begin()+i+1, v.end(), vrijednost);
		v.erase(it, v.end());
		i += 1;
	}
	for (int a : v) {
		cout << a << " ";
	}
	

}

int main() {
	vector <int> v = { 1,12,9,4,1,9 };
	funkcija(v);
	return 0;
}
